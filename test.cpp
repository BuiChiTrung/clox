#include <cctype>
#include <iostream>
#include <map>

using namespace std;
// std::map<int, int> a = {{1, 1}};
// map<int, char> m = {{1, 'a'}, {3, 'b'}, {2, 'c'}};

int main() {
  // std::string a = "abc\rabc";
  // std::cout << a[1] << " " << a[2] << std::endl;
  // char x = '_';
  // std::cout << std::isalpha(x) << std::endl;
  if (1 == 1 and "abcaaaaaaaaaaaaaaaaaaaa" == "abcaaaaaaaaaaaaaaaaaaaa" and
      2 == 2) {
    std::cout << "alo";
  }
}
